<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.inventory_pj.mapper.HallInvenMapper">

	<select id="orderData" resultType="org.inventory_pj.model.Order_detailVO">
		<!-- SELECT * FROM order_detail WHERE order_num IN ( SELECT order_num FROM order_total WHERE regdate BETWEEN DATE_SUB(CURDATE(), INTERVAL (WEEKDAY(CURDATE()) 
			+ 3) % 7 DAY) AND DATE_ADD(CURDATE(), INTERVAL (7 + 3 - WEEKDAY(CURDATE())) % 7 DAY) ) -->
		SELECT food, SUM(cnt) AS total_cnt
		FROM order_detail
		WHERE order_num IN (
		SELECT order_num
		FROM order_total
		WHERE regdate BETWEEN
		DATE_SUB(CURDATE(), INTERVAL (WEEKDAY(CURDATE()) + 3) % 7 DAY)
		AND
		DATE_ADD(CURDATE(), INTERVAL (7 + 3 - WEEKDAY(CURDATE())) % 7 DAY)
		)
		GROUP BY food
	</select>
</mapper>
