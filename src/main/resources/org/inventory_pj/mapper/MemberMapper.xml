<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.inventory_pj.mapper.MemberMapper">

	<!-- 로그인 -->
	<select id="signin" resultType="org.inventory_pj.model.MemberVO">
		select * from member
		where username = #{username}
	</select>
</mapper>